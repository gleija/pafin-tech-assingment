import { style } from "@vanilla-extract/css";

export const titleContainer = style({
  display: "flex",
});

export const titleText = style({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  gap: "12px",
});

export const titleContainerSmall = style({
  display: "flex",
});

export const titleTextSmall = style({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  gap: "12px",
});

export const content = style({
  display: "flex",
  alignItems: "center",
});
